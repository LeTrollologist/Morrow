// ==========================================================
// Generated by forge bindgen — Tungsten C-ABI Stable Bridge
// ==========================================================

// Constants (from C #define)
// const SQLITE_OK: i64 = 0;
// const SQLITE_ERROR: i64 = 1;
// const SQLITE_VERSION: String = "3.45.1";

// C-Compatible Struct Layouts
#[repr(C)]
struct SQLiteHandle {
    fd: i32,
    state: i32,
}

// Foreign Function Interface Declarations
extern "C" {
    fn strlen(s: *const u8) -> usize;
    fn puts(s: *const u8) -> i32;
    fn malloc(size: usize) -> *mut u8;
    fn free(ptr: *mut u8);
    fn sqlite3_open(filename: *const u8, db: *mut SQLiteHandle) -> i32;
    fn sqlite3_close(db: *mut SQLiteHandle) -> i32;
}
