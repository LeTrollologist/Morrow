[package]
name = "rust_tokio"
version = "0.1.0"
edition = "2021"

[workspace]
[dependencies]
tokio = { version = "1", default-features = false, features = ["rt-multi-thread", "net", "io-util"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
